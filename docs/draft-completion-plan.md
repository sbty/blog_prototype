# 複数ブログ下書きの完成計画

更新: 2026-09-08。実装・現在の検証結果を過去の計画より優先する。

## 完成条件

- 複数ブログへ下書きを保存できる。
- 同じ記事の重複作成を防ぐ。
- 失敗後に再実行しても成功済み・結果不明の記事を重複作成しない。
- 公開承認、下書きと予約投稿の分離、対象・状態・永続化検証を維持する。

## 初期確認

| 分類 | 根拠・判断 |
| --- | --- |
| 実装済み | `BatchExecutionService` は複数ブログの逐次処理、部分失敗集計、STOPを実装。`DraftSaveService` は許可ブログ、同名検索、保存証跡、新コンテキスト監査を実装。関連単体テストあり。 |
| 未完成 | 新規作成の永続的な排他記録がなく、保存結果不明・一覧反映遅延・並行実行を同名検索だけでは防げない。 |
| 未検証 | 現在の全チェック、障害後の再実行の回帰試験、現在のBlogger UIによる複数ブログ実保存。 |
| 保全対象 | 開始時の画像アップローダーとそのテストの変更、既存HANDOFF変更。今回のcommitへ混入させない。 |
| 過去情報との差 | HANDOFFのブランチ名・roadmapの広範な未整理状態は現状と不一致。実際の開始ブランチは `codex/verified-draft-pipeline`。 |

## 作業順序と進捗

1. [x] Git・ルート手順・関連実装とテスト・HANDOFFを確認。
2. [x] 継続運用をAGENTSへ保存し、矛盾する関連手順を最小修正。
3. [x] 新規作成直前の永続的な排他記録を追加。ブログIDとslug/titleで重複を拒否し、結果不明でも記録を保持。
4. [x] 複数ブログ、成功後・失敗後・検索反映遅延・再起動後・別DB接続からの同時実行を回帰試験で検証。
5. [x] 関連51テストに合格。ライブ診断後の画像表示待ち修正は関連19テストと `scripts/verify-agent.ps1`（80ファイル・625テスト、lint、型検査）に合格。差分レビュー済み。
6. [x] `8db5e69`（永続排他・運用計画）、`8e46ec7`（画像表示待ち）をcommit・push。[PR #87](https://github.com/sbty/blog_prototype/pull/87)を作成。
   最新CIの確認は接続障害で未完了。マージは必須チェック・無競合・非デプロイ確認後に行う。
7. [ ] 実保存の検証と残る制限を区別して報告。対象未指定・既存修復の個別承認待ちは推測して変更しない。

## 再実行の安全な意味

保存済み・結果不明の項目は新規作成を拒否する。結果不明を自動成功扱いせず、
既存下書きの正確なIDを読取監査で確定して、既存の承認付き更新経路を使う。
新規作成を呼ぶ前の読取失敗は再実行可能。排他記録の自動解除や期限切れは行わない。
ローカルDBを共有しない別端末や手動投稿との排他は今回保証しない。
導入前の作成履歴は自動登録しないため、従来のBlogger同名監査も維持する。
作成を呼んだ後は、実際に保存されなかった場合も安全側で停止する。
DBの排他記録を削除して再試行する運用は追加しない。

## 次の操作・制限

専用ブランチは `codex/draft-retry-safety`、PRのベースは既存機能ブランチ
`codex/verified-draft-pipeline`。既存5コミットは今回のPR差分に含めない。
開始時の画像変更はライブ障害に直接関係するため、内容を保全して
表示待ち修正と同じ作業単位へ統合した。既存HANDOFFはローカルに保全。

ユーザーはテスト対象の選択を任せ、下書き保存を許可した。既存設定から
2ブログを選び、品質監査済み素材に検証専用タイトル・slugを付けて実行。
新たな費用・公開・予約・削除は発生させていない。

- 1ブログ目: 画像挿入前にCompose選択肢未検出で停止。直後の同名件数は0だが、
  診断HTMLに投稿IDがある。正確なIDの完全監査は本文未検出でUNVERIFIED。
  新規作成記録は保持。再作成や記録解除はしない。
- 原因修正: 非表示要素のDOM存在だけで待機を終えていた処理を、表示待ちへ修正。
  非表示の重複要素と遅延表示の回帰試験を追加。実画像挿入の成功は未検証。
- 2ブログ目: 初回はスキップ。修正後に独立実行したが、保存前の一覧読込で
  接続タイムアウト。事後監査も同じ接続障害でUNVERIFIED。保存成功は0件。
- GitHubも同時期に接続タイムアウト。最初のcommitのCIは2ジョブ成功、
  画像修正後の最新CIは未確認。未マージ。

再開時はGit状態、PR #87の最新CI、Blogger接続を確認する。
ローカルHANDOFFの正確な対象・成果物を使い、1ブログ目はまず読取監査で
既存投稿の状態を確定する。確認不能なまま新規作成・既存更新をしない。
2ブログ目は保存前に失敗しているため、接続回復後の事前監査に合格した場合、
既存の許可範囲で未完了項目だけを再実行する。同じ外部障害を反復しない。
実環境で2ブログの独立再読込検証がPASSになるまで完成条件は未達。

## 今回の範囲外

自動公開、予約機能拡張、有料生成、既存記事の自動修復、別端末間の分散排他。
