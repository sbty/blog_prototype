# Examples and fixtures

- Examples are sanitized, runnable schema demonstrations. Use fake blog IDs,
  post IDs, admin URLs, public URLs, and credentials.
- Keep examples aligned with current parsers and required fields, but do not turn
  real operational artifacts from `data` into public samples.
- When a schema changes, update only affected examples and run their existing
  validation/tests.
